import React from "react";
import "./instructions.css";

function Instructions(props) {
    return (
        <div class="inst">
            <h1>Clicky Game</h1>
            <h3 class="inst">Click on an image to earn a point, but don't click on it more than once!</h3>
        </div>
    );
}

export default Instructions;
